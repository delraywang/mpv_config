# 在媒体文件所在目录生成的 Windows 快捷方式名称（不含 .lnk）；! 前缀使其在正序排列时靠前。
shortcut_name=!继续之前播放

# 便携版 mpv.exe 的位置；~~home/../mpv.exe 表示 portable_config 的上一级。
mpv_executable=~~home/../mpv.exe

# 自然播放结束时回退的秒数，避免点击快捷方式后立即再次结束。
end_rewind=10

# 是否在文件结束、停止或手动退出时生成入口。
enabled=yes
create_on_end=yes
create_on_shutdown=yes
